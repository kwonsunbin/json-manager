# PRD.md — Product Requirements Document

## 프로젝트 개요

JsonManager는 콘솔 환경에서 JSON 파일 기반으로 데이터를 관리하는 CRUD 애플리케이션이다.
데이터는 `data/items.json`에 영속 저장되며, 프로그램 재시작 후에도 유지된다.

---

## 기능 요구사항

### FR-01. Create — 데이터 생성

- 사용자는 이름(name)과 설명(description)을 입력하여 새 항목을 생성할 수 있다.
- 이름은 필수 입력이다. 빈 값 입력 시 오류 메시지를 출력하고 생성을 중단한다.
- 설명은 선택 입력이다. 빈 값 허용.
- ID는 시스템이 자동 부여한다. 기존 최대 ID + 1의 정수 값.
- 생성 시 createdAt, updatedAt은 현재 시각으로 자동 설정된다.

### FR-02. Read — 데이터 조회

- 사용자는 저장된 전체 항목 목록을 조회할 수 있다.
- 저장된 항목이 없으면 "저장된 데이터가 없습니다." 메시지를 출력한다.
- 출력 형식: `[ID: {id}] {name} | {description} (생성: {createdAt} / 수정: {updatedAt})`

### FR-03. Search — 데이터 검색

- 사용자는 키워드로 항목을 검색할 수 있다.
- 검색 대상: ID(완전 일치), 이름(부분 일치), 설명(부분 일치)
- 대소문자를 구분하지 않는다.
- 검색어가 빈 값이면 오류 메시지를 출력하고 검색을 중단한다.
- 결과가 없으면 "검색 결과가 없습니다." 메시지를 출력한다.

### FR-04. Update — 데이터 수정

- 사용자는 ID를 입력하여 특정 항목의 name 또는 description을 수정할 수 있다.
- 존재하지 않는 ID 입력 시 오류 메시지를 출력하고 수정을 중단한다.
- 수정 가능 필드: name, description
- 잘못된 필드 선택 시 오류 메시지를 출력한다.
- 새로운 값이 빈 값이면 오류 메시지를 출력한다.
- 수정 성공 시 updatedAt이 현재 시각으로 갱신된다. createdAt은 변경되지 않는다.

### FR-05. Delete — 데이터 삭제

- 사용자는 ID를 입력하여 특정 항목을 삭제할 수 있다.
- 존재하지 않는 ID 입력 시 오류 메시지를 출력하고 삭제를 중단한다.
- 삭제 실행 전 삭제 대상을 출력하고 사용자 확인(y/n)을 받는다.
- `y` 이외의 입력은 삭제를 취소한다.

---

## 비기능 요구사항

### NFR-01. 데이터 영속성

- 모든 변경 사항은 즉시 `data/items.json`에 반영된다.
- 프로그램 재시작 후에도 기존 데이터가 유지된다.

### NFR-02. 동시성 안전

- Create, Update, Delete는 동기화(synchronized)되어 Race Condition을 방지한다.

### NFR-03. 데이터 무결성

- JSON 저장 전 ID, name, createdAt, updatedAt 값의 무결성을 보장한다.
- 기존 데이터의 createdAt은 절대 변경되지 않는다.

### NFR-04. 오류 처리

- 파일 I/O 오류 발생 시 RuntimeException으로 래핑하여 상위로 전달한다.
- 예외를 무시하지 않는다.

### NFR-05. 환경

- Java 17, Gradle 9.3, Gson 2.11.0
- OS 독립적으로 동작한다.

---

## 사용자 시나리오

### 시나리오 1. 새 항목 추가

```
메뉴 선택 > 1
[Create] 새 데이터 추가
  이름: 노트북
  설명: MacBook Pro 14인치 M4
[완료] 데이터가 저장되었습니다.
  → [ID: 1] 노트북 | MacBook Pro 14인치 M4 (생성: 2026-06-11 12:00:00 / 수정: 2026-06-11 12:00:00)
```

### 시나리오 2. 전체 목록 조회

```
메뉴 선택 > 2
[Read] 전체 목록
  총 2건
  ────────────────────────────────────────────────────────────
  [ID: 1] 노트북 | MacBook Pro (생성: ... / 수정: ...)
  [ID: 2] 마우스 | MX Master (생성: ... / 수정: ...)
```

### 시나리오 3. 키워드 검색

```
메뉴 선택 > 3
[Search] 데이터 검색
  검색어 (ID 또는 이름/설명 키워드): 노트
  검색 결과: 2건
  ────────────────────────────────────────────────────────────
  [ID: 1] 노트북 | MacBook Pro (생성: ... / 수정: ...)
  [ID: 3] 노트패드 | Samsung (생성: ... / 수정: ...)
```

### 시나리오 4. 항목 수정

```
메뉴 선택 > 4
[Update] 데이터 수정
  수정할 데이터 ID: 1
  현재 데이터: [ID: 1] 노트북 | MacBook Pro (생성: ... / 수정: ...)
  수정할 필드: 1. 이름 / 2. 설명
  선택 > 2
  새로운 값: MacBook Pro 14 M4
[완료] 데이터가 수정되었습니다.
  → [ID: 1] 노트북 | MacBook Pro 14 M4 (생성: ... / 수정: 2026-06-11 12:05:00)
```

### 시나리오 5. 항목 삭제

```
메뉴 선택 > 5
[Delete] 데이터 삭제
  삭제할 데이터 ID: 1
  삭제할 데이터: [ID: 1] 노트북 | MacBook Pro (생성: ... / 수정: ...)
  정말 삭제하시겠습니까? (y/n): y
[완료] 데이터가 삭제되었습니다.
```

---

## 예외 처리 정책

| 상황 | 처리 |
|------|------|
| Create 시 이름 빈 값 | 오류 메시지 출력 후 메뉴로 복귀 |
| Search 시 키워드 빈 값 | 오류 메시지 출력 후 메뉴로 복귀 |
| Update/Delete 시 존재하지 않는 ID | 오류 메시지 출력 후 메뉴로 복귀 |
| Update 시 잘못된 필드 선택 | 오류 메시지 출력 후 메뉴로 복귀 |
| Update 시 새 값 빈 입력 | 오류 메시지 출력 후 메뉴로 복귀 |
| Delete 확인 미동의 (y 외 입력) | 삭제 취소 후 메뉴로 복귀 |
| 파일 I/O 실패 | RuntimeException 발생 (상위로 전달) |
| 메뉴 외 번호 입력 | 오류 메시지 출력 후 재입력 대기 |
